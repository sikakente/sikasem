# API base URL — point this at your Railway backend in production.
# For local development, leave as-is (uses docker-compose backend).
# For production builds (EAS Build), set this in the EAS dashboard:
#   eas.json → build → production → env → EXPO_PUBLIC_API_URL
EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1
